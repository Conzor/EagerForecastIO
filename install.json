{
  "resources": {
    "head": [
      {
        "type": "style",
        "src": "./app.css"
      },
      {
        "type": "script",
        "src": "./build/app.js"
      }
    ]
  },
  "preview": {
    "handlers": [
      {
        "options": ["*"],
        "execute": "INSTALL_SCOPE.setOptions(INSTALL_OPTIONS)"
      }
    ]
  },
  "options": {
    "properties": {
      "element": {
        "description": "Where do you want the forecast to appear?",
        "title": "Forecast location",
        "order": 0,
        "type": "object",
        "format": "element",
        "default": {
          "selector": "body",
          "method": "prepend"
        }
      },
      "colors": {
        "title": "Colors",
        "order": 1,
        "type": "object",
        "properties": {
          "tempColor": {
            "default": "#000000",
            "format": "color",
            "order": 0,
            "title": "Temperature bar",
            "type": "string"
          },
          "backgroundColor": {
            "default": "#ffffff",
            "format": "color",
            "order": 1,
            "title": "Background",
            "type": "string"
          }
        }
      },
      "font": {
        "description": "What font should be used for the forecast?",
        "order": 2,
        "placeholder": "Helvetica",
        "title": "Font",
        "type": "string"
      },
      "units": {
        "default": "us",
        "description": "What units should be used for temperature and wind speed?",
        "order": 3,
        "title": "Units",
        "type": "string",
        "enum": [
          "us",
          "uk",
          "si",
          "ca"
        ],
        "enumNames": {
          "us": "Fahrenheit and mph",
          "uk": "Celsius and mph",
          "si": "Celsius and m/s",
          "ca": "Celsius and km/h"
        }
      }
    }
  }
}

